# **АЛГОРИТМ ЧУДНОВСКИХ (CHUDNOVSKY) ПОЛНЫЙ РАЗБОР**

## **1. МАТЕМАТИЧЕСКАЯ ОСНОВА**

### **1.1. Основная формула (1989 год)**
```
             ∞
1/π = 12 ∑  (-1)^k * (6k)! * (545140134k + 13591409)
         k=0  --------------------------------------
               (3k)! * (k!)^3 * (640320)^(3k + 3/2)
```

**Где:**
- `k` - индекс члена ряда (от 0 до ∞)
- `!` - факториал
- Все вычисления проводятся с **высокой точностью** (GMP библиотека)

### **1.2. Упрощенная версия для вычислений**
```
π = [426880 * √10005] / ∑_{k=0}^{∞} T_k
```
где:
```
T_k = (-1)^k * (6k)! * (545140134k + 13591409)
      --------------------------------------
      (3k)! * (k!)^3 * (640320)^(3k)
```

**Константы:**
```
C = 426880 * √10005 ≈ 426880 * 100.024996875781
J = 545140134
M = 13591409
L = 640320^3 = 262537412640768000
```

---

## **2. ПОЛНЫЙ ВЫВОД ФОРМУЛЫ**

### **2.1. Исходные функции**
Чудновские использовали **модулярные формы** и **функции Эйзенштейна**:

**j-инвариант:**
```
j(τ) = 1728 * [E_4(τ)^3] / [E_4(τ)^3 - E_6(τ)^2]
```
где τ = i (мнимая единица)

**Для τ = (1 + i√163)/2 получаем:**
```
j(τ) = -640320^3
```
Отсюда и возникает магическое число 640320.

### **2.2. Связь с π через гипергеометрические функции**
Формула выводится из:
```
π = [Γ(1/2)]^2 = интеграл
```
Используя:
```
2F1(a,b;c;z) = ∑_{k=0}^{∞} [(a)_k (b)_k / (c)_k] * z^k / k!
```
где `(a)_k` - символ Похгаммера (возрастающий факториал)

### **2.3. Финальный вывод**
```
1/π = 12 ∑_{k=0}^{∞} (-1)^k * (6k)! * (A + Bk)
                         ---------------------
                         (3k)! * (k!)^3 * C^{3k+3/2}
```
где:
```
A = 13591409
B = 545140134
C = 640320
```

---

## **3. АЛГОРИТМ ВЫЧИСЛЕНИЯ ПОШАГОВО**

### **Шаг 1: Инициализация констант**
```python
# Точность в десятичных цифрах
DIGITS = 1000000
# Константы (должны быть высокой точности)
A = 13591409
B = 545140134
C = 640320
C3_OVER_24 = C**3 // 24  # 640320^3 / 24
```

### **Шаг 2: Рекуррентные соотношения**
Вместо вычисления факториалов каждый раз используем рекуррентные формулы:

**Для члена T_k:**
```
T_0 = 1
T_k = T_{k-1} * [-(6k-5)(2k-1)(6k-1)] / [k^3 * C3_OVER_24]
```
где множитель `-(6k-5)(2k-1)(6k-1)` получен из:
```
(6k)! / (6(k-1))! = 6k*(6k-1)*(6k-2)*...*(6k-5)
(3k)! / (3(k-1))! = 3k*(3k-1)*(3k-2)
(k!)^3 / ((k-1)!)^3 = k^3
```

**Упрощенный множитель:**
```
M_k = (6k-5)(2k-1)(6k-1) * 24
```

### **Шаг 3: Вычисление суммы S**
```
S = A  # при k=0
for k = 1 to N:
    T_k = T_{k-1} * [-(6k-5)(2k-1)(6k-1)] / [k^3 * C3_OVER_24]
    S += T_k * (A + B*k)
```

### **Шаг 4: Вычисление π**
```
pi = [426880 * √10005] / S
```
где √10005 вычисляется с высокой точностью.

---

## **4. ОПТИМИЗИРОВАННЫЙ АЛГОРИТМ (С РЕКУРРЕНТНЫМИ МНОЖИТЕЛЯМИ)**

### **4.1. Полный псевдокод**
```
function compute_pi_chudnovsky(digits):
    # 1. Установка точности
    precision = digits + 10  # запас
    set_precision(precision)
    
    # 2. Константы
    A = 13591409
    B = 545140134
    C = 640320
    C3_OVER_24 = C^3 / 24
    
    # 3. Инициализация
    T = 1        # T_0
    S = A        # S_0
    k = 1
    
    # 4. Вычисление суммы
    while abs(T * (A + B*k)) > epsilon:
        # Вычисляем множитель для T_k
        M = (6*k-5) * (2*k-1) * (6*k-1)
        T = T * (-M) / (k^3 * C3_OVER_24)
        
        # Добавляем к сумме
        S += T * (A + B*k)
        k += 1
    
    # 5. Вычисление π
    sqrt_10005 = sqrt(10005, precision)
    pi = (426880 * sqrt_10005) / S
    
    return pi
```

### **4.2. Параллельная версия**
**Разбиение ряда на блоки:**

**Для K потоков:**
```
Поток i вычисляет сумму от k_start до k_end:
    S_i = Σ_{k=k_start}^{k_end} T_k * (A + B*k)
    
где T_k вычисляется независимо в каждом потоке:
    T_k = [(-1)^k * (6k)! * (A + B*k)] / [(3k)! * (k!)^3 * C^(3k)]
```

**Проблема:** Нужно вычислять факториалы с высокой точностью для каждого k.

**Решение:** Вычислять T_k через рекуррентные соотношения от начального значения.

---

## **5. МАТЕМАТИЧЕСКИЕ ОПТИМИЗАЦИИ**

### **5.1. Бинарное разбиение (Binary Splitting)**
Самый эффективный метод для вычисления длинных рядов:

**Идея:** Разбить сумму на две части и вычислять независимо:
```
S(a,b) = Σ_{k=a}^{b-1} T_k * (A + B*k)
```

**Рекурсивно:**
```
S(a,b) = S(a,m) + T(a,m) * S(m,b)
```
где `T(a,m)` - произведение коэффициентов от a до m-1.

### **5.2. Матричная форма**
Член ряда можно представить как:
```
T_k = P(k) / Q(k)
```
где:
```
P(0) = 1
Q(0) = 1
P(k) = -(6k-5)(2k-1)(6k-1) * P(k-1)
Q(k) = k^3 * C3_OVER_24 * Q(k-1)
```

Тогда:
```
π = [426880√10005] / Σ_{k=0}^{∞} [P(k)/Q(k)] * (A + B*k)
```

### **5.3. Высокоуровневый алгоритм с бинарным разбиением**
```
function binary_split(a, b):
    if b - a == 1:
        if a == 0:
            P = 1
            Q = 1
        else:
            P = (-1)^a * (6a-5)(2a-1)(6a-1)
            Q = a^3 * C3_OVER_24
        return (P, Q, A + B*a)
    else:
        m = (a + b) // 2
        (P_am, Q_am, S_am) = binary_split(a, m)
        (P_mb, Q_mb, S_mb) = binary_split(m, b)
        P_ab = P_am * P_mb
        Q_ab = Q_am * Q_mb
        S_ab = Q_mb * S_am + P_am * S_mb
        return (P_ab, Q_ab, S_ab)

# Вычисление π
(P, Q, S) = binary_split(0, N)
pi = (426880 * √10005 * Q) / (S + A*Q)
```

---

## **6. ПАРАЛЛЕЛЬНЫЕ РЕАЛИЗАЦИИ**

### **6.1. Метод 1: Блочное разбиение ряда**
```
# Разбиваем ряд на M блоков
Блок i: k от i*(N/M) до (i+1)*(N/M)-1

Каждый поток:
    1. Вычисляет начальное T_k для своего блока
    2. Вычисляет частичную сумму S_i
    3. Возвращает S_i

Главный поток:
    1. Суммирует все S_i
    2. Вычисляет π
```

### **6.2. Метод 2: Дерево бинарного разбиения**
```
Уровень 0:        [0, N)
                 /       \
Уровень 1:   [0, N/2)   [N/2, N)
               /   \     /   \
Уровень 2: [0,N/4) ... [3N/4, N)

Каждый узел дерева вычисляется отдельным потоком.
```

### **6.3. Метод 3: Конвейерная обработка**
```
Поток 1: Вычисляет P(k), Q(k) для k
Поток 2: Умножает P(k) на (A + B*k)
Поток 3: Делит на Q(k)
Поток 4: Суммирует результаты
Поток 5: Вычисляет окончательное π
```

---

## **7. ПОЛНАЯ МАТЕМАТИЧЕСКАЯ ВЕРИФИКАЦИЯ**

### **7.1. Доказательство сходимости**
**Скорость сходимости:** Каждый член дает ~14.18 десятичных цифр
```
digits_per_term = log10(640320^3 / 24) ≈ 14.18
```

**Для N членов:** точность ≈ 14.18 * N цифр.

**Пример:** Для 1 миллиона цифр нужно:
```
N = 1000000 / 14.18 ≈ 70521 членов ряда
```

### **7.2. Оценка погрешности**
Остаток ряда после N членов:
```
|R_N| < (12 * (6N)! * (A + BN)) / ((3N)! * (N!)^3 * C^(3N+3/2))
```

**График сходимости:**
```
N=1:    8 верных цифр
N=2:    23 верных цифры
N=3:    37 верных цифр
N=10:   142 верных цифры
N=100:  1418 верных цифр
```

---

## **8. РЕАЛИЗАЦИЯ НА ЯЗЫКЕ C++ С GMP**

```cpp
#include <gmpxx.h>
#include <iostream>
#include <cmath>

void chudnovsky(unsigned long digits) {
    mpf_set_default_prec(digits * 3.33); // биты = цифры * log2(10)
    
    // Константы
    mpz_class A = 13591409;
    mpz_class B = 545140134;
    mpz_class C = 640320;
    mpz_class C3_OVER_24 = C * C * C / 24;
    
    // Инициализация
    mpz_class P = 1;
    mpz_class Q = 1;
    mpz_class K = 0;  // множитель (A + B*k)
    mpz_class S = A;  // начальная сумма
    
    mpz_class k = 1;
    mpz_class term;
    
    while (true) {
        // Вычисляем множитель для P
        mpz_class M = (6*k-5) * (2*k-1) * (6*k-1);
        
        // Обновляем P и Q
        P *= -M;
        Q *= k * k * k * C3_OVER_24;
        
        // Вычисляем K
        K = A + B * k;
        
        // Вычисляем член ряда
        term = (P * K) / Q;
        
        // Проверяем сходимость
        if (term == 0) break;
        
        // Добавляем к сумме
        S += term;
        k++;
    }
    
    // Вычисляем π
    mpf_class pi;
    mpf_class sqrt_10005;
    mpf_sqrt_ui(sqrt_10005.get_mpf_t(), 10005);
    
    pi = (426880 * sqrt_10005) / S;
    
    // Вывод результата
    std::cout << "π с точностью " << digits << " цифр:" << std::endl;
    gmp_printf("%.Ff\n", pi.get_mpf_t());
}
```

---

## **9. ИСТОРИЧЕСКИЙ КОНТЕКСТ И АНАЛОГИ**

### **9.1. Сравнение с другими формулами**
| Формула | Цифр за член | Автор | Год |
|---------|-------------|-------|-----|
| **Лейбниц:** π/4 = 1 - 1/3 + 1/5 - 1/7 + ... | 0.1 | Лейбниц | 1674 |
| **Мачин:** π/4 = 4arctan(1/5) - arctan(1/239) | 1.4 | Мачин | 1706 |
| **Рамануджан:** 1/π = (2√2/9801)∑(4k)!(1103+26390k)/(k!)^4*396^(4k) | 8.0 | Рамануджан | 1914 |
| **Чудновские:** наша формула | 14.2 | Чудновские | 1989 |

### **9.2. Почему именно 640320?**
Число возникает из теории **эллиптических кривых**:
```
j( (1 + i√163)/2 ) = -640320^3
```
где 163 - число Хегнера (одно из чисел, дающих целый j-инвариант).

### **9.3. Рекорды вычислений**
- **1989:** Чудновские - 1 млрд цифр
- **2002:** Токио - 1.24 трлн цифр
- **2009:** Такахиро - 2.7 трлн цифр
- **2019:** Google - 31.4 трлн цифр
- **2021:** Швейцария - 62.8 трлн цифр

Все рекорды используют **вариации алгоритма Чудновских**.

---

## **10. МАТЕМАТИЧЕСКИЕ СЛЕДСТВИЯ И ОБОБЩЕНИЯ**

### **10.1. Обобщенная формула**
Для произвольной точности d:
```
1/π = 12 ∑_{k=0}^{∞} (-1)^k * (6k)! * (A_d + B_d*k)
                        --------------------------
                        (3k)! * (k!)^3 * C_d^(3k+3/2)
```
где A_d, B_d, C_d зависят от требуемой скорости сходимости.

### **10.2. Связь с другими константами**
Аналогичные формулы существуют для:
- **e** (число Эйлера)
- **ln(2)** (натуральный логарифм 2)
- **ζ(3)** (постоянная Апери)

### **10.3. Теоретическая значимость**
Алгоритм Чудновских доказывает:
1. **π - вычислимое число** с линейной сложностью O(N log N)
2. **Связь теории чисел и анализа**
3. **Эффективность модулярных форм** в вычислениях

---

## **ВЫВОД:**

**Алгоритм Чудновских - оптимальный выбор для архиватора потому что:**
1. **Скорость:** 14.18 цифр за член ряда
2. **Точность:** Абсолютная (при использовании GMP)
3. **Параллелизуемость:** Библиотеки бинарного разбиения
4. **Проверенность:** Все рекорды π используют его
5. **Стабильность:** Четкие критерии остановки

**Для архиватора:** Вычислять π один раз с максимальной точностью (100+ млн цифр), сохранить на SSD, и использовать как базу данных для сжатия.